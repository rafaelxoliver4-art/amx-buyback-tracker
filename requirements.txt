# Light dependencies only. Anything heavier needs an explicit YES from the owner.
requests>=2.31
pdfplumber>=0.11
PyYAML>=6.0
pandas>=2.0
openpyxl>=3.1
matplotlib>=3.8          # Cycle 1: output/amx_buybacks_chart.png

# tests
pytest>=8.0
